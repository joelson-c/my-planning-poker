{
  "include": ["src/**/*.ts", "bindings/**/*.ts"],
  "extends": "@planningpoker/tsconfig",
  "compilerOptions": {
    // Remix takes care of building everything in `remix build`.
    "noEmit": true
  }
}
